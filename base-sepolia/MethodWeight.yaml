eth_blockNumber: # 方法名
  # 需要注意的是，一旦这个方法设置了任意一项的级别，当它在和其他级别搭配时候，没有设置的级别按照 默认 100 处理。
  # 比如我只设置了： N100 的权重，其实隐含 其他权重都是 100
  # 不必纠结是否总和为 100 ，程序会按照比例去计算。
  # 比如  N100 的权重 50 其他没有设置，N100 的比例在所有级别节点都有情况下应该是： 50/650 ，以此类推。
  # 一个级别范围的权重，是需要附加到节点上的，比如 N100 的权重 50，有 5 台节点，其实 N100 的最终权重应该是 250
  # 数据范围必须是 0 - n
  N200: 100
  N300: 1000
